import React from 'react';
import './NoticeSection.css'; // Assuming you will create a CSS file for specific styles

const NoticeSection = () => {
    return (
        <div className="notice-section">
            <p className="notice-text">
                *Geographical restrictions apply. The gift card is only valid in select regions.
            </p>
        </div>
    );
};

export default NoticeSection;